name: pip_domain
description: >
  Modello di dominio della Personal Intelligence Platform, indipendente da Flutter,
  Supabase o da qualsiasi provider AI (Architectural Principles, Principio 1;
  Engineering Constitution, Articolo 1/4).
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  pip_shared:
    path: ../shared

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
